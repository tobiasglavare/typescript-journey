// Optional properties: Create a User interface
// with required name/email and optional 
// phone/address.

interface user {
    name: string;
    email: string;
    phone?: string;
    address?: string;
}